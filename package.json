{
  "name": "cortex-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Cortex Engine - Autonomous Multi-Agent Intelligence & Computer Runtime",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
